apply plugin : 'java-library'

group = 'ca.concordia.jddown'

version = '1.0'

sourceCompatibility = targetCompatibility = compileJava.sourceCompatibility = compileJava.targetCompatibility = '1.8'

repositories {
    jcenter()
}

dependencies {
    testImplementation 'junit:junit:4.13'
}

test {
    ignoreFailures = true
}
